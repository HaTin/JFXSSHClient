/**
 * Service 层（业务逻辑）。
 *
 * <p>承载面向 UI 的业务接口与实现。已实现：ConnectionService、GroupService、
 * SettingsService、CredentialVault（凭据加密）、ConnectionPortService（导入导出）。
 * 规划中：SessionManager（见 docs/API.md）。</p>
 */
package com.xxx.jfxssh.service;
